# Prisma / Postgres connection string
# Update the host/user/password/database to match your environment.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/llm_visi_dash?schema=public"

# All webhook-related values (URL, basic auth credentials, timeout, etc.)
# are stored in the database via the Settings screen. Keep this file free of secrets.

# Optional: Configure a direct AI provider to replace the external webhook (if set). If
# AI_PROVIDER_URL and AI_PROVIDER_API_KEY are set, the server will invoke that provider
# for AI calls instead of forwarding to target webhook URLs. The API key should include
# any needed Bearer or other prefix (e.g. "Bearer <token>").
AI_PROVIDER_URL="https://url.agents.do-ai.run/api/v1/chat/completions"
AI_PROVIDER_API_KEY="Bearer my-secret-api-key" #note Bearer is required before the api key
